# Multi-Agent Feature Development Configuration

# System Requirements (must be installed):
# - Claude Code CLI (claude-code --version)
# - GitHub CLI (gh auth status)
# - Git with worktree support

# Optional: GitHub token (if different from gh CLI)
GITHUB_TOKEN=your-github-token-here

# Optional: Custom reviewer profiles (comma-separated)
REVIEWER_PROFILES="frontend,backend,devops"

# Optional: Max concurrent tasks (default: 2)
MAX_CONCURRENT_TASKS=2

# Optional: Required approvals per task (default: 3) 
REQUIRED_APPROVALS=3

# Optional: Max retry attempts per task (default: 3)
MAX_ATTEMPTS=3

# Optional: Claude Code timeout (milliseconds, default: 300000)
CLAUDE_CODE_TIMEOUT=300000

# Paths (will be auto-detected if not specified)
# MAIN_REPO_PATH=/path/to/your/main/repo
# BASE_WORKTREE_PATH=/path/to/worktree/parent

# API Server Configuration
API_PORT=3456
API_HOST=localhost

# Authentication
# Generate a secure token with: openssl rand -hex 32
API_TOKEN=your-secure-api-token-here

# State Management
STATE_DIR=.codettea/state

# Logging
LOG_LEVEL=info
NODE_ENV=development

# CORS Configuration (comma-separated list)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100